{% extends 'base.html.twig' %}

{% block body %}

<div class="mainContainer">
    <h1>Soil creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('admin_soil_index') }}">Back to the list</a>
        </li>
    </ul>
</div>
{% endblock %}
